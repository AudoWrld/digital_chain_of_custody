# rps_game

Simple Rock-Paper-Scissors game with a small CLI and unit tests.

Run interactively:

```bash
python -m rps_game.cli 3   # play 3 rounds interactively
```

Run tests (uses pytest):

```bash
python -m pip install -U pytest
pytest -q
```

This package uses only the Python standard library.
